<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Pos.ListeArticle"
             Title="Articles">


    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="TbAdd" Text="Add"  Clicked="TbAdd_Clicked" />
        <ToolbarItem x:Name="TbEdit" Text="Edit"  Clicked="TbEdit_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout>
            <SearchBar x:Name="txtArt" Placeholder="Recherche..." />
            <ListView x:Name="lsArt">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ImageCell 
                            Text="{Binding ArtName}" 
                            Detail="{Binding Price}"
                            ImageSource="{Binding ImgName}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Label Text="bar..." VerticalOptions="End" HorizontalOptions="FillAndExpand" BackgroundColor="red" HeightRequest="25" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>