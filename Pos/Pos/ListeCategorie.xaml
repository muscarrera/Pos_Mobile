<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Pos.ListeCategorie"
             Title="ListeCategorie">
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="TbAdd" Text="Add"  Clicked="TbAdd_Clicked" />
        <ToolbarItem x:Name="TbEdit" Text="Edit"  Clicked="TbEdit_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="lsCat">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding catName}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>